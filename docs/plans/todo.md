# TODO (ai-rules)

- Wire Claude hook bundle into a sample project (document steps + example `settings.json` merge).
- Add Ash/Ecto-specific checks to Claude hooks and `mix quality` (e.g., missing preloads, unsafe Repo calls in LiveView handlers).
