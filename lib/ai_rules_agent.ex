defmodule AiRulesAgent do
  @moduledoc """
  Entry point for ai_rules_agent package (design sketch).

  To run the HTTP dev server in a host project:

      AI_AGENT=1 MIX_ENV=dev mix run scripts/ai/serve_agent.exs

  To run stdio server:

      AI_AGENT=1 MIX_ENV=dev mix run scripts/ai/serve_agent_stdio.exs
  """
end
